Parser:

На входе текстовый файл с кодом и метками в комментариях вроде @brief, @detailed и т.п.
Комментарии с описанием включают в себя метки:
@brief - короткое описание объекта
@detailed - подробное описание объекта
@param - описания параметра передающегося в функцию
@todo - описание того что нужно сделать или переделать в объекте


Под каждую функцию, класс, структуру создается отдельная html страница (или динамически составляется)
Блоки требующие отдельных страниц описания:
- класс, структура
- функция
- перечисление
- ???файл

???создавать экземпляры класса DescriptionPage, с векторами описаний (для параметров и тудушек). добавлять туда описания, складировать их в другом векторе, а в конце "публиковать" в хтмл
???

Страница описания объекта - общий шаблон страницы с автоматически сгенерированным описанием блоков, на основе комментариев в коде.
Содержит:
1) Имя описываемого объекта
2) Ссылки на файлы содржащие прототип и реализацию объекта описания
3) Тектовые поля сгенерированые на основе @brief и @detailed меток в комментариях
4) Таблицу @todo меток и их текстового описания
???5) Список файлов в которых встречается данный объект
Реализуется как класс-родитель для более специализированных страниц описания классов, структур, функций и т.п.

Страница описания класса - потомок общего шаблона страниц описания с добавленными специфичными для класса полями и секциями.
Дополнительно содержит:
1) Список предков/интерфейсов класса (???с кратким описанием)
2) Список методов класса с их кратким описанием
???3) Список атрибутов класса с их кратким описанием, если оно есть

Страница описания функции - потомок общего шаблона страниц предназначенный для описания функций и методов класса.
Дополнительно содержит:
1) Таблицу @param меток и их текстового описания
2) Ссылка на класс которому принадлежит метод, если он есть

Страница описания перечисления - потомок общего шаблона страницы описания
Дополнительно содержит:
1)